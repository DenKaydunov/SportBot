spring:
  application:
    name: SportBot
  cloud:
    gcp:
      sql:
        database-name: sportbot_db
        instance-connection-name: sunny-might-465815-m2:us-central1:sportbotdb
  datasource:
    username:
    password:
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: false

  liquibase:
    enabled: true
    change-log: db/changelog/db.changelog-master.yaml