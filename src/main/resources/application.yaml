spring:
  application:
    name: SportBot
  cloud:
    gcp:
      sql:
        database-name: sportbot_db
        instance-connection-name: sportbot-479615:europe-west1:sportbot
  datasource:
    username: postgres
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: false

  data:
    web:
      pageable:
        one-indexed-parameters: true

  liquibase:
    enabled: true
    change-log: db/changelog/db.changelog-master.yaml