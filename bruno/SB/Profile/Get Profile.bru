meta {
  name: Get Profile
  type: http
  seq: 1
}

get {
  url: {{url}}/api/v1/profile?telegramId={{testTelegramId}}
  body: none
  auth: inherit
}

params:query {
  telegramId: {{testTelegramId}}
}

settings {
  encodeUrl: true
  timeout: 0
}
